Extract the table data from the image. Return only the table data in CSV (Comma-Separated Values) format. The first row must be the header row containing the column names. Each row after the header should represent the rows of the table. Do not return any additional text.